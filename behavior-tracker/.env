# Frontend Environment Variables
VITE_API_URL=http://localhost:3001

# Backend Environment Variables
DATABASE_URL=postgresql://seo@localhost:5432/behavior_tracker
JWT_SECRET=your-256-bit-secret-key-here-change-in-production
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:5173

# HIPAA Compliance Requirements
# ⚠️  CRITICAL: This application handles PHI (Protected Health Information)
# - Never commit real secrets to version control
# - Use environment-specific secret managers in production
# - Ensure BAA (Business Associate Agreement) with all cloud providers
# - Enable TLS 1.2+ for all connections (DATABASE_URL should use SSL)
# - Audit logs are append-only and track all PHI access
# - Client data is de-identified (codes + initials only)